#include <stdio.h>

int main() {
    int radius = 4;
    float pi = 3.14;
    printf("area of circle is :%f meter^2\n", pi*radius*radius);
    int height = 4;
    printf("volume of cylinder is: %f meter^3",pi*radius*radius*height);

    return 0;
}